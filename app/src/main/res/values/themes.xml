<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.ReshalaAlfa01" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_reshala_logo_light</item>
        <item name="windowSplashScreenBackground">@color/splash_bg_light</item>
        <item name="postSplashScreenTheme">@style/Theme.ReshalaAlfa01</item>
    </style>
</resources>